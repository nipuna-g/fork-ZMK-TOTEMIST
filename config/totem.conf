CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Combo buffers. Defaults are 4 and 5; raised here to leave headroom,
# since combo_caps_word uses two keys that are also home row mods.
CONFIG_ZMK_COMBO_MAX_KEYS_PER_COMBO=4
CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=8
